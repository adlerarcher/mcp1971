# MCP 1971

Read-only permitting coordination timeline. Captured Model Context Protocol responses, labeled provenance, deterministic critical path. Not an official product of the U.S. Department of Energy or any agency.

Live: [mcp1971.thermalunderground.org](https://mcp1971.thermalunderground.org)

## DNS

At the `thermalunderground.org` registrar, add:

```
mcp1971    CNAME    adlerarcher.github.io
```

In this repo, GitHub Pages should use the custom domain `mcp1971.thermalunderground.org` with Enforce HTTPS. The `CNAME` file in the root is that domain.
